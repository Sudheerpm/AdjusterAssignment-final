apiVersion: v1
kind: Service
metadata:
    name: sudheerpmadjusterassignmentfinal
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sudheerpmadjusterassignmentfinal